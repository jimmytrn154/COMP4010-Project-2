## 🔧 Installation & Usage
*(Note to student: Fill in your specific instructions below)*

1.  **Clone the Repo:**
    ```bash
    git clone https://github.com/jimmytrn154/COMP4010-Project-2.git
    ```
2.  **Create your branch for working:**
    ```bash
    git checkout -b your-branch-name
    #Example: git checkout -b chuong

    #2.2 Write those lines after making changes
    git add .
    git commit -m "message"
    git push origin your-branch-name
    ```
3.  **Install Dependencies:**
    ```bash
    pip install -r requirements.txt
    ```
4.  **When other user had changes:**
    ```bash
    git checkout main
    git pull
    git checkout -b new-branch-name

    #After changes continue the step 2.2
    ```
---
**Developed by:** Group 3 | **Course:** COMP4010 | **Date:** May 2026